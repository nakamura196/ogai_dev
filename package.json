{
  "private": true,
  "scripts": {
    "build": "tinacms build && nuxt build",
    "build_": "nuxt build",
    "dev_": "nuxt dev --dotenv .env.local",
    "dev": "tinacms dev -c \"nuxt dev\"",
    "dev:prod": "nuxt dev --dotenv .env.production",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate:local": "nuxt generate --dotenv .env.local",
    "generate_": "nuxt generate --dotenv .env.production",
    "__generate": "tinacms build && nuxt generate --dotenv .env.production",
    "generate": "tinacms build && nuxt generate --dotenv .env.local",
    "generate:stg": "nuxt generate --dotenv .env.stage"
  },
  "devDependencies": {
    "@mdi/js": "^7.3.67",
    "@nuxt/content": "^2.9.0",
    "@nuxtjs/i18n": "^8.0.0-beta.10",
    "@tinacms/cli": "^1.5.39",
    "@types/node": "^20.10.5",
    "cross-env": "^7.0.3",
    "nuxt": "3.8.2",
    "sass": "^1.69.5",
    "vue-tsc": "^1.8.25"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "CETEIcean": "^1.8.0",
    "splitpanes": "^3.1.5",
    "tinacms": "^1.5.28",
    "vuetify": "^3.4.6"
  }
}
